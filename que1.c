#include <stdio.h>
int main()
{
    // create a program that determines whether a given year is leap year or not

    int leapYear;
    printf("Enter your year : ");
    scanf("%d", &leapYear);

    if(leapYear == )

    return 0;
}